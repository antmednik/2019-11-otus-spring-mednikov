spring:
  shell:
    interactive:
      enabled: false

  datasource:
    url: jdbc:h2:mem:myDb;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
    schema: schema.sql
    data: data.sql
    initialization-mode: embedded
